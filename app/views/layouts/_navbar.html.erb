<div>
  <h3 class="float-md-start mb-0">Cover</h3>
  <nav class="nav nav-masthead justify-content-center float-md-end">
    <a class="nav-link fw-bold py-1 px-0 active" aria-current="page" href="#">Home</a>
    <a class="nav-link fw-bold py-1 px-0" href="#">Features</a>
    <a class="nav-link fw-bold py-1 px-0" href="#">Contact</a>
    <% if user_signed_in? %>
        <%= link_to 'Edit Perfil', edit_user_registration_path, class: 'nav-link fw-bold py-1 px-0' %>
        <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: 'nav-link fw-bold py-1 px-0' %>
      <% else %>
        <%= link_to t('.sign_up'), new_user_registration_path, class: 'nav-link fw-bold py-1 px-0' %>
        <%= link_to t('.sign_in'), new_user_session_path, class: 'nav-link fw-bold py-1 px-0' %>
      <% end %>
  </nav>
</div>